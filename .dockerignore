# Version control / CI
.git
.github
.gitignore

# Dependencies & build output (rebuilt inside the image)
node_modules
dist

# Secrets & local env
.env
.env.*
!.env.example

# Editor / tooling
.vscode
.idea
.hintrc
.editorconfig

# Docs & specs (not needed at runtime)
docs
*.md
!README.md

# Local Lavalink runtime artifacts
docker/lavalink/application.yml
**/*.jar
**/*.jar.bak

# Logs & local databases
logs
*.log
prisma/*.db
prisma/*.db-journal
